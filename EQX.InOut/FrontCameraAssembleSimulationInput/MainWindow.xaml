<Window x:Class="Simulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Simulation"
        mc:Ignorable="d"
        xmlns:cvt ="clr-namespace:FrontCameraAssembleSimulationInput.Converters"
        ResizeMode="CanResize"
        Title="FrontCameraAssembleSimulationInputMainWindow">      
    <Window.Resources>
        <cvt:InputOutputListConverter x:Key="InputOutputListConverter" />
        <cvt:BooleanToFillOnOffConverter x:Key="BooleanToFillOnOffConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Set Input Origin"
                Command="{Binding SetInputOrigin}" />

            <Button Content="Set Input Run"
                Command="{Binding SetInputRun}" />

            <Button Content="Off All Inputs"
                Command="{Binding OffAllInputsCommand}"/>
        </StackPanel>
        <GroupBox Header="Input"
              Grid.Row="1">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <Button Content="?"
                        Command="{Binding InputDeviceIndexDecrease}" />
                    <TextBlock Text="{Binding SelectedInputDeviceIndex, StringFormat='{}Input Board #{0}'}"
                           Grid.Column="1"
                           TextAlignment="Center"
                           FontWeight="Bold"
                           FontSize="18"
                           VerticalAlignment="Center" />
                    <Button Content="?"
                        Command="{Binding InputDeviceIndexIncrease}"
                        Grid.Column="2" />
                </Grid>

                <ItemsControl>
                    <ItemsControl.ItemsSource>
                        <MultiBinding Converter="{StaticResource InputOutputListConverter}">
                            <Binding Path="DataContext.InputServer.Inputs"
                                 RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=Window}" />
                            <Binding Path="DataContext.SelectedInputDeviceIndex"
                                 RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=Window}" />
                        </MultiBinding>
                    </ItemsControl.ItemsSource>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2 0 10 0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto"
                                                  SharedSizeGroup="InOutIndexColumn" />
                                    <ColumnDefinition Width="7*" />
                                </Grid.ColumnDefinitions>

                                <Ellipse Width="30"
                                     Height="30"
                                     Fill="{Binding Value,Converter={StaticResource BooleanToFillOnOffConverter}}"
                                     Stroke="LightGray"
                                     StrokeThickness="1">
                                </Ellipse>
                                <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Text="{Binding Id,StringFormat={}[{0}]}"
                                       Margin="1 0" />
                                <Button Content="{Binding Name}"
                                    Click="Button_Click"
                                    Grid.Column="2" />
                            </Grid>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
