<UserControl x:Class="EQX.FlowUILibraryWPF.MVVM.Views.ConvertColorToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EQX.FlowUILibraryWPF.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Width"
                    Value="150" />
            <Setter Property="VerticalContentAlignment"
                    Value="Center" />
        </Style>
        <Style TargetType="ToolTip">
            <Setter Property="Foreground"
                    Value="Red" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border BorderBrush="LightGray"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <TabControl>
                    <TabItem Header="Parameters">

                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Color Conversion" />
                                <ComboBox ItemsSource="{Binding ColorConversionCodeList}"
                                      Width="400"
                                      SelectedItem="{Binding Parameters[ColorConversionCode],Mode=TwoWay}">
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="Results">
                        <StackPanel Orientation="Vertical">
                        </StackPanel>
                    </TabItem>
                </TabControl>
                <Label Content="{Binding Cost}"
                               Grid.Row="1"
                               HorizontalAlignment="Left"/>
            </Grid>
        </Border>
        <TabControl Grid.Column="1">
            <!--<TabItem Header="Output">
                <flowUILibraryViews:ImageDisplayer ImageMat="{Binding DataContext.Outputs[ImageMat],ElementName=root}" />
            </TabItem>-->
        </TabControl>
    </Grid>
</UserControl>

