<Window x:Class="EQX.FlowUILibraryWPF.MVVM.Views.AddFlowWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EQX.FlowUILibraryWPF.MVVM.Views"
        mc:Ignorable="d"
        Title="AddFlowWindowView" Height="200" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Height="50">
            <Label Content="Flow Name" VerticalAlignment="Center"/>
            <TextBox Text="{Binding FlowName}"
                     Width="300"
                     Height="30"/>
        </StackPanel>
        <UniformGrid Grid.Row="1">
            <Button Content="OK" Click="Button_OK_Click"/>
            <Button Content="Cancel" Click="Button_Cancel_Click"/>
        </UniformGrid>
    </Grid>
</Window>
