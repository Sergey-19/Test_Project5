<UserControl x:Class="EQX.FlowUILibraryWPF.MVVM.Views.ConnectionPathView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EQX.FlowUILibraryWPF.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Delete"
                      Command="{Binding DeletePathCommand}" />
        </ContextMenu>
    </UserControl.ContextMenu>

    <Path x:Name="connectionPath"
          Data="{Binding GeometryPathData}"
          Stroke="Gray"
          StrokeThickness="2">
    </Path>
</UserControl>
